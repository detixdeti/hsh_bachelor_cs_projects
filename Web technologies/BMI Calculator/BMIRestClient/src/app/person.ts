export class Person{
    weight: number;
    height: number;
    bmi: number;
    answer: string;

    constructor (weight: number, height: number, bmi: number, answer:string){
        this.weight = weight;
        this.height = height;
        this.bmi = bmi;
        this.answer = answer;
    }
}